import './reset.css';

const Home = ({ onLoginButtonClick }: { onLoginButtonClick: () => void }) => {
  //todo
  return (
    <>
      <h1>todo</h1>
      <button onClick={onLoginButtonClick}></button>
    </>
  );
};

export default Home;
